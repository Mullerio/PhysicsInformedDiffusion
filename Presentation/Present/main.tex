\documentclass[10pt]{beamer}
\usetheme{Boadilla} %CHANGE THIS FOR DIFFERENT COLOR ETC.
\usepackage{xcolor, soul}
\sethlcolor{red}
\usepackage[absolute,overlay]{textpos}
\usepackage{dsfont}
\usepackage{array}
\usepackage{tikz}
\usetikzlibrary{shapes, arrows.meta, positioning}
\usepackage[style=alphabetic]{biblatex}  
\addbibresource{lib.bib}
\usepackage{caption}
\usepackage{xcolor}
\usepackage{mathrsfs}
\usepackage{mathtools}
\usepackage{comment}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{float} 

\newtheoremstyle{reminder}% Custom style for reminders
  {3pt} % Space above
  {3pt} % Space below
  {\itshape} % Body font
  {} % Indent amount
  {\bfseries\color{blue}} % Theorem head font
  {.} % Punctuation after theorem head
  {.5em} % Space after theorem head
  {} % Theorem head spec (can be left empty, meaning `normal`)

\theoremstyle{reminder}
\newtheorem{reminder}{Reminder}

\author[Jonas Müller]{Jonas Müller}

\setbeamertemplate{enumerate item}{\arabic{enumi}.}  % Use regular numbers and a period

%%%%%%%%%%%%%%%%%% Begin of the Presentation %%%%%%%%%%%%%%%%%

\begin{document}

\title[Physics Informed Diffusion Models]{Physics Informed Diffusion Models}
\institute{}
\date{}

\begin{frame}
    \titlepage
\end{frame}

%%%%%%%%%%%%%%%%%% Outline %%%%%%%%%%%%%%%%%%%%%
\section{Recap Diffusion Models}

\section{Setup: Physics Informed Diffusion Models}

\section{Training Algorithm}

\section{Experiments}
\section{Discussion}

\begin{frame}
    \frametitle{Outline}
    \tableofcontents
\end{frame}

%%%%%%%%%%%%%%%%%% Beginning of slides %%%%%%%%%%%%%%%%%

\begin{frame}{Recap Diffusion Models}
        Diffusion models want to optimize $$\arg \max_\theta \mathbb{E}_{x_0 \sim p_{data}}\left[\log p_\theta(x_0)\right]$$
        and do so by learning the backward OU or a denoising process, with a loss like $$L(\theta) = \mathbb{E}_{t \sim U([0,T]), x_0 \sim p_{data}}\left[\lambda_t \| x_0 - \hat{x}_0(x_t, t)\|^2\right]$$
    \begin{figure}[h]
\centering
\includegraphics[width=0.7\linewidth]{images/2026-01-22-23-53-56.png}
\caption{Forward and backward SDE in diffusion models.}
\label{fig:main-2026-01-22-23-53-56}
\end{figure}
\end{frame}

\begin{frame}
    \begin{center}
        \large
        How can we enforce physical constraints?
    \end{center}
\end{frame}

\begin{frame}{Physics Setup}
    \begin{definition}
        We assume our physics is described by a PDE on $\Omega \subseteq \mathbb{R}^d$ of the form 
        \[
            \mathcal{F}\left(u(\xi)\right) = 0 \quad \text{with} \quad \xi \in \Omega. 
        \]
        and we have a boundary operator 
        \[\mathcal{B}\left(u(\xi)\right) = 0 \quad \text{with} \quad \xi \in \partial \Omega.\]
        where $u : \Omega \rightarrow \mathbb{R}^c$ is the solution of interest. To simplify, we write \[R(u(\xi)) = [\mathcal{F}(u(\xi)), \mathcal{B}(u(\xi))]^\top\].
    \end{definition}
    \pause
    They only consider $$[1,...,n] \times [1,...,n] \subseteq \mathbb{Z}_n \times \mathbb{Z}_n.$$
    Similar to standard image based diffusion models, data is then considered to be given as $x_0 \in \mathbb{R}^{c \times n \times n}$ and $\mathcal{F}, \mathcal{B}$ are discretized accordingly.
\end{frame}



\begin{frame}{Setting up Physics based Training Objective}
    The residual $\mathcal{R} = 0$ in the non discrete gives us an infinite amount of information. 
    To utilize this, we introduce a loss based on $\mathcal{R}$ via a \textit{virtual observable}.
    $$q_{\mathcal{R}}(\cdot \mid x_0) \sim \mathcal{N}(\mathcal{R}(x_0), \sigma^2 I_d)$$

    \pause

    \begin{definition}
        Define $$p_\theta(\hat{r}) = \int_{\Omega} q_{\mathcal{R}}(\hat{r} \mid x_0) p_\theta(x_0) dx_0$$
        then we can optimize 
        $$ \arg \max_\theta \mathbb{E}_{\hat{r} \sim \delta_0}\left[\log p_\theta(\hat{r})\right]=\arg \max _\theta \mathbb{E}_{x_0 \sim p_\theta}\left[\log q_{\mathcal{R}}\left(\hat{r}=0 \mid x_0\right)\right] .$$
    \end{definition}
    \pause

    So we can train our model by evaluating $x_0$ on the the virtual observable log likelihood.
    Note that this loss is equivalent to $$\arg \max_\theta \log p_\theta(0) = \arg \max _\theta \mathbb{E}_{x_0 \sim p_\theta}\left[\log q_{\mathcal{R}}\left(0 \mid x_0\right)\right]$$
\end{frame}

\begin{comment}
    
\begin{frame}
\begin{figure}[h]
\centering
\includegraphics[width=0.8\linewidth]{images/2026-01-06-00-39-43.png}
\caption{}
\label{fig:main-2026-01-06-00-39-43}
\end{figure}
\end{frame}

\end{comment}


\begin{frame}{Introducing the Data Loss}
    How can we also accommodate a data loss? \\
    \pause
\begin{definition}
Thus the objective is given by
\[
\arg\max_{\theta}\Bigg(
\underbrace{
\mathbb{E}_{x_0 \sim p_{data}}\!\left[\log p_\theta(x_0)\right]
}_{\text{data likelihood term}}
\;+\;
\underbrace{
\mathbb{E}_{x_0 \sim p_\theta}\!\left[\log q_{\mathcal{R}}\!\left(0 \mid x_0\right)\right]
}_{\text{physics likelihood term}}
\Bigg)
\]
\end{definition}

\pause

2 main questions arise from this objective: \\

\begin{enumerate}
    \item How do we optimize this objective? Sampling from $p_\theta$ is expensive. 
    \item How should we choose the $\sigma$ in $q_{\mathcal{R}}$?
\end{enumerate}
\pause

We would want something like $$L(\theta) = \mathbb{E}_{t \sim U([0,T]), x_0 \sim p_{data}}\left[\lambda_t \| x_0 - \hat{x}_0(x_t, t)\|^2 + \mathcal{R}(x_0^*(x_t, t))\right]$$

where $x_0^*(x_t, t)$ should be some approximation of a sample from $p_\theta$ given $x_t$.
\end{frame}

\begin{frame}{Problem 1: Mean Estimation}
    In classical diffusion training we have estimates $$\hat{x}_0(x_t, t) \approx \mathbb{E}[x_0 \mid x_t]$$
    Then, in the classic loss we directly use that in our loss. 
    However, it holds $$\mathcal{R}(\hat{x}_0(x_t, t)) \approx \mathcal{R}(\mathbb{E}[x_0 \mid x_t]) \neq \mathbb{E}[\mathcal{R}(x_0)  \mid  x_t]$$
    Where $$R(\hat{x}_0(x_t, t)) - \mathbb{E}[\mathcal{R}(x_0)  \mid  x_t] \propto \text{Var}(x_0 \mid x_t) = \Sigma_t$$

    \bigbreak
    $\quad \longrightarrow$ Mitigate this effect by a appropriate choice of variance for $q_\mathcal{R}$. \\
\end{frame}


\begin{frame}{Heuristic Solution to Mean Estimation}
    To mitigate this, they increase the variance as $t$ increases. They choose 
        $$q_{\mathcal{R}}(\cdot \mid x_0, t) = \exp \left(- \frac{1}{2\Sigma_t} \|\mathcal{R}(x_0)\|^2 \right) \sim \mathcal{N}(\mathcal{R}(x_0), \Sigma_t I_d)$$
        With this, the physics loss will be more impactful for small $t$. Why? \\
    \pause    
        $$–\log q_{\mathcal{R}}(0 \mid x_0, t) =  \frac{1}{2\Sigma_t} \|\mathcal{R}(x_0)\|^2 + \text{const}$$
    \begin{center}
    \begin{tabular}{|c|c|}
        \hline
        \textbf{Small $t$} & \textbf{Large $t$} \\
        \hline
        $\Sigma_t$ small & $\Sigma_t$ large \\
        $1/\Sigma_t$ large & $1/\Sigma_t$ small \\
        error small & error large \\
        weight large & weight small \\
        \hline
    \end{tabular}
    \end{center}
\end{frame}


\begin{frame}{Sample estimation DDIM Sampling}
    Full inference via DDPM is expensive, mean estimation might not be accurate :( \\
    $\quad \longrightarrow$ Use DDIM sampling to get $x_0 \sim p_\theta$ on which to evaluate the physics loss! \\

    \begin{center}
        \begin{figure}[h]
\centering
\includegraphics[width=0.8\linewidth]{images/2026-02-11-00-14-31.png}
\caption{With DDIM we don't need to run the full backwards process.}
\label{fig:main-2026-02-11-00-14-31}
\end{figure}
    \end{center}
The authors propose to use DDIM sampling with only a few steps to get a approximate sample $x_0^*$of $p_\theta$. 
\end{frame}



\begin{frame}{Full Training Algorithm}

\begin{algorithm}[H]
\caption{Physics-informed diffusion model training}
\label{alg:pidm}
\begin{algorithmic}[1]
\Repeat
    \State Sample $x_0 \sim p_{\text{data}}, t \sim \text{Uniform}\{1, \ldots, T\}, \epsilon \sim \mathcal{N}(0, I)$
    \State Compute
    \[
        x_t = \sqrt{\bar{\alpha}_t}\, x_0 + \sqrt{1 - \bar{\alpha}_t}\, \epsilon
    \]
    \State Estimate $x_0^*$ via
        $\hat{x}_0 \approx \mathbb{E}[x_0 \mid x_t]$
    or DDIM sampling. 
    \State Take a gradient descent step on
    \[
        \nabla_\theta \Bigg[
        \lambda_t \left\| x_0 - \hat{x}_0(x_t, t) \right\|^2
        + c \cdot \frac{1}{2\Sigma_t}
        \left\| \mathcal{R}\!\left(x_0^*(x_t, t)\right) \right\|^2
        \Bigg]
    \]
\Until{converged or for fixed number of iterations}
\end{algorithmic}
\end{algorithm}
\pause
$c > 0$ is a hyperparameter which controls the weight of the physics loss. \\
\textbf{Note:} The physics loss is just the log likelihood of the virtual observable!
\end{frame}






\begin{frame}{Experimental Setup}
    They consider only one set of PDEs, namely the Darcy Flow equations given by 
\begin{align*}
u(x) &= - K(x) \nabla p(x)  \quad &\text{ Constitutive law} \\
-\nabla \cdot u(x) &= f(x) \quad &\text{ Conservation of mass}
\end{align*}
    where $K$ is the permeability field, $p$ the pressure field and $f$ a source term. They describe fluid flow through porus media.
\pause
    \begin{figure}[h]
\centering
\includegraphics[width=0.5\linewidth]{images/vecteezy_diagonal-cleaning-sponge-photo_65421531.png}
\caption{A porus medium (sponge).}
\label{fig:main-2026-01-06-22-54-59}
\end{figure}
\end{frame}
  \begin{frame}{Experimental Results}
    \begin{figure}[h] 
\centering
\includegraphics[width=0.8\linewidth]{images/2026-01-07-12-46-34.png}
\caption{Difference between standard diffusion models and physics informed version.}
\label{fig:main-2026-01-07-12-46-34}
\end{figure}
\end{frame}

\begin{frame}
    \begin{figure}[h]
\centering
\includegraphics[width=0.8\linewidth]{images/2026-02-12-16-26-07.png}
\caption{Traing and residual loss comparison between standard and physics informed diffusion models.}
\label{fig:main-2026-02-12-16-26-07}
\end{figure}
    
\end{frame}

\begin{frame}{Discussion}
     \begin{itemize}
        \item They only consider 2d data and PDEs on $\mathbb{R}^2$, how this works on higher dimensions is not tested. \pause
        \item The hyperparameter $c$ for the variance is crucial, choosing it too large leads to an overfocus on the residual and thus not sampling from the entire data distribution.
    \end{itemize}
    \begin{figure}[h]
\centering
\includegraphics[width=0.8\linewidth]{images/2026-01-25-01-34-15.png}
\caption{Self implemented toy example, where too large $c$ leads to bad results.}
\label{fig:main-2026-01-25-01-34-15}
\end{figure}
\end{frame}


%%%%%%%%%%%%%%%%%% End of Presentation %%%%%%%%%%%%%%%%%

\end{document}